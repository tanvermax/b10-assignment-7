

const Selectedplayer = ({handleshowallPlayer}) => {
    return (
        <div>
            <h4>hi</h4>
            <button className="btn" onClick={handleshowallPlayer}>Addmore more player</button>
        </div>
    );
};

export default Selectedplayer;